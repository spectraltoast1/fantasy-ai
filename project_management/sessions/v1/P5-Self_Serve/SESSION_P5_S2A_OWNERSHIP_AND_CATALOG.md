# V1 · P5 · Session S2a — Ownership + the scoped catalog — the brief that was run

**Status: SHIPPED + AUDITED + DEPLOYED 2026-08-09 — ENDORSED.** Kept as the record of what was executed.
**Report:** `SESSION_P5_S2A_REPORT.md` · **Audit:** `SESSION_P5_S2A_AUDIT.md` (7 findings, none blocking;
they are distributed across S2b / S2c / S2d).
**Was** `SESSION_P5_S2_OWNERSHIP_AND_ISOLATION.md`, which bundled four sessions into one file. Split
2026-08-11 (Will): **one brief per session.** → `SESSION_P5_S2B_SCOPED_READS.md` ·
`SESSION_P5_S2C_PUNCH_LIST.md` · `SESSION_P5_S2D_DEMO_CLONE_AND_SELECTOR.md`.
**Project:** `projects/v1/P5_ACCOUNTS_SELF_SERVE_ONBOARDING.md` · **Prior:** `SESSION_P5_S1B_AUDIT.md`.

> **What this session does:** makes an account *mean* something. S1/S1b built a front door; every read
> behind it is still open to everyone, and an unauthenticated request still resolves to **Will's real
> league** by name-resolution accident. S2 gives leagues owners, scopes every read to the caller, and
> proves a stranger cannot reach someone else's league.

> **Why it was split.** This is the security work, and the standing instruction is *do not let
> a fast cadence compress it — isolation bugs fail silently.* One session that both invents the ownership
> model and rewrites every read's authorization ends with a green run and no way to tell which half the
> green came from. **S2a built the model and closed the catalog — that is *discovery*. S2b closes the
> eleven per-panel reads — that is *access*.**

---

## The settled model (decided with Will, 2026-08-05 — do not re-derive)

**Visibility is one predicate:**

```
visible(league) = (league_id == DEMO_LEAGUE_ID)  OR  (owned by caller  AND  season == current)
```

- **There is exactly ONE public league** — the demo. Everything else is private and requires ownership.
  This inverts the old model: the 31 demo slices were a *demonstration that multi-league works*, not a
  product surface.
- **`DEMO_LEAGUE_ID` is a config value, not a table.** `application/config.py` + a Fly secret or `[env]`,
  resolved env-first exactly like `ACCESS_CODE`. One value, no migration, no seeding, and it cannot drift
  from a loader-generated artifact. It points at real LoRP 2025 (`1182101676608823296`) **today** and gets
  repointed at the anonymized clone when that exists — one line, whenever.
- **"Current season" comes from Sleeper's `/v1/state/nfl`** (`season`), not a hardcoded constant. It rolls
  over during the preseason, which is the desired behaviour — 2026 was already "current" in early August.
- **The demo is the deliberate exception to the current-season rule** — it is a 2025 league in the 2026
  season. Do **not** implement visibility as a global `season = current` filter; the demo term comes first
  and is season-independent. Implemented as a global filter, the demo vanishes and it looks like an auth bug.
- **The 31 corpus slices stay in the database.** They are engineering fixtures — `compute_demo_slices`,
  `build_demo_manifest`, B6's 31/31 verification and `check_scoped_reload`'s parity oracle all depend on
  them. **Only the public catalog shrinks to one.** Nothing is deleted.

**What people see:**

| state | lands on | can switch to |
|---|---|---|
| signed out | the demo (it is also the landing page for now) | nothing else |
| signed in, ≥1 owned current-season league | **their own league** | the demo, and their other leagues |
| signed in, no league yet | the demo (this is the empty state) | — |

The demo staying reachable after sign-in is deliberate: S5 declines out-of-scope leagues, so a user can
hold an account with nothing of their own to look at.

**The season selector goes.** Prior seasons are corpus, not product — there is no user value in browsing
last year. `season` is **not a SQL filter anywhere** in the read layer (verified: zero hits; a redraft
`league_id` already pins one `(league, season)` slice), so removing it is a frontend + catalog change and
touches no data path. The **league** switcher stays — a user may own several current-season leagues. The
**week** switcher stays.

## Decisions I made for you (Code: follow unless you hit a reason not to)

1. **Ownership is a hand-written table.** `public.user_leagues (user_id uuid references auth.users on
   delete cascade, league_id text, created_at)`, PK `(user_id, league_id)`, in `api/auth_schema.sql`
   beside `app_users` and `signup_attempts`. **Not** in `serve/schema.sql`: that file is generated by
   `--emit` and applied by `--load`, which DROPs every table it names. This project has already lost
   `ros_player_band`'s RLS to exactly that.
2. **No `league_visibility` table.** An earlier draft of this brief proposed one; it is unnecessary once
   there is exactly one public league. Config beats a table here.
3. **Authorization is one predicate, in one place** — `slice_params`, the dependency every read already
   takes. Not repeated per route. One seam, one place to get it wrong, one place to test.
4. **An unowned league returns the SAME 404 as a league that does not exist.** `slice_params` already 404s
   an unknown `league_id`; route the unauthorized case into that identical response. A 403 would confirm
   the league exists, which is an enumeration oracle — Sleeper league IDs are guessable, so a 403/404 split
   lets anyone probe which leagues are on the platform. Same reasoning that made S1b's refusal uniform.
5. **Signed-out resolves to `DEMO_LEAGUE_ID` explicitly.** Today `league_id=None` falls through
   `MY_USERNAME` to the is_mine league — an anonymous visitor lands on Will's real league by accident of
   name resolution. Make it explicit. `MY_USERNAME` stays only as the resolver for Will's own viewer seat.
6. **`viewer_roster_id` stays caller-supplied but must be validated against a visible slice.** Viewing your
   own league "as" another manager is a feature the dossiers depend on; reaching into a league you cannot
   see is the bug. Validate the slice, not the seat.
7. **Grant/revoke is operator tooling now, the connect flow later.** `scripts/users.py` gains
   `--grant <email> <league_id>` and `--revoke`. S4's connect flow will write the same row; S2 must not
   wait on S4 to have data to test with.
8. **Do not build RLS policies.** Settled: isolation is **API-layer**. The app connects as an owner role
   that bypasses RLS, so a policy build denies nothing today and reads as protection that isn't there.
   (S2c still fixes the *emit* drift — that is about not silently losing a property, not relying on it.)

## Your part, Will (~5 min)

1. **A second email you can receive at** — a `+tag` on your own inbox is fine now that custom SMTP
   delivers to non-team addresses. S2a needs two real accounts: one account proves a gate exists, it
   cannot prove the gate separates people.
2. **The eyeball at S2b:** watch Code sign in as user B and fail to reach user A's league — by URL, not
   just by UI.

---

## The brief to paste to Code — S2a

```
Goal: V1 Project 5, Session S2a (projects/v1/P5_ACCOUNTS_SELF_SERVE_ONBOARDING.md) — give leagues OWNERS
and make the catalog answer per caller. Step one of the security session; scoping the individual reads is
S2b and is explicitly OUT of scope here.

Read first: sessions/v1/P5-Self_Serve/SESSION_P5_S2_OWNERSHIP_AND_ISOLATION.md (the settled model at the
top is not up for re-derivation), SESSION_P5_S1B_AUDIT.md, context/CODING_BIBLE.md, SESSION_GUIDE.md.
Check the brief against observable reality before executing — S1's failure chain started with a documented
intent that no longer matched the live system.

Build:
- public.user_leagues (user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE, league_id text,
  created_at timestamptz default now(), PRIMARY KEY (user_id, league_id)) in api/auth_schema.sql — NOT in
  serve/schema.sql, which --emit regenerates and --load DROPs. Add it to init_auth_schema's table list.
- DEMO_LEAGUE_ID as a CONFIG value (application/config.py + Fly env), resolved env-first exactly like
  ACCESS_CODE in api/settings.py. No table. Set it to 1182101676608823296 (LoRP 2025) for now; it will be
  repointed at an anonymized clone later, and that must remain a one-line change.
- Current season resolved from Sleeper's /v1/state/nfl `season`, not a hardcoded constant. Cache it
  sensibly; it changes once a year.
- scripts/users.py: --grant <email> <league_id> and --revoke <email> <league_id>. Keep --list/--ban/
  --unban; --list should show what each user owns.
- Scope /api/leagues to the caller: the demo always, plus the caller's own current-season leagues via
  user_leagues. Signed-out returns the demo only. This is the ONE unscoped read today and it is the
  catalog, so it is the right first thing to close.
- Make the signed-out default slice DEMO_LEAGUE_ID explicitly, rather than resolving through MY_USERNAME
  to the is_mine league. MY_USERNAME stays as the resolver for Will's own viewer seat.
- Catalog ordering: a caller's own leagues come first, the demo last, so the frontend default is "first
  entry" and a signed-in user with a league lands on THEIR league rather than the demo.

Prove it (demonstrated, not asserted) — with TWO real accounts, not one:
1. Signed-out GET /api/leagues returns exactly the demo — count it, don't eyeball it.
2. User A signed in sees the demo PLUS the league granted to A, A's league first, and no others.
3. User B signed in sees the demo PLUS B's league, and NOT A's. Revoke B's grant; show it disappear.
4. A league granted to A but from a PRIOR season does not appear — the current-season term bites.
5. The DEMO is visible in every state including signed-out, and is NOT filtered out by the current-season
   term (it is a 2025 league in the 2026 season — this is the case a naive `season = current` filter
   breaks, so prove it explicitly).
6. --grant is idempotent; --revoke of a non-existent grant is a clean no-op.
7. Deleting a user cascades their user_leagues rows away, leaving no orphans.
8. Parity: every OTHER read's payload is unchanged for the signed-out default and for user A —
   value-identical, per-endpoint. Say value-identical, not byte-identical, unless you are comparing bytes.

Scope guard — does NOT: scope the twelve per-panel reads or move authorization into slice_params (S2b);
remove the season selector from the frontend (S2b, with the catalog shape change); build RLS policies;
touch the connect flow, jobs table, or worker; touch any pipeline, transform, loader, or engine constant;
touch the frozen corpus; DELETE any of the 31 corpus slices from the database — they stay as fixtures.

Follow SESSION_GUIDE.md: fresh worktree, <=3 commits, update STATUS.md + ARCHITECTURE.md (an ownership
model is a stack-level fact), then close/merge/push. This touches application/api/* — so it needs a
REDEPLOY and a live confirmation on https://surplusff.com/. A merged-but-undeployed change has bitten this
project (P0/B3) and a session forgot the deploy line as recently as 2026-08-05.
```

## Definition of done (S2a)

1. `user_leagues` exists, applied by `init_auth_schema`, and is absent from the generated `serve/schema.sql`
   — asserted, as `app_users` already is.
2. `DEMO_LEAGUE_ID` resolves env-first and repointing it is one config change.
3. Two real accounts, two different catalogs, demonstrated — including the revoke and the prior-season case.
4. The demo is visible in every state, including signed-out, and survives the current-season term.
5. Signed-out `/api/leagues` returns exactly the demo; a signed-in user with a league gets their own first.
6. `--grant` / `--revoke` work, are idempotent, and `--list` shows ownership.
7. Every other read is value-identical for signed-out and for user A.
8. Merged, **redeployed**, confirmed live.

## Notes / gotchas

- **There is no second league to test with until you make one.** Grant user B a corpus league that is not
  the demo. Decide it early — "we couldn't test cross-user isolation" is not an acceptable outcome here.
- **`slice_exists` is doing double duty today** — it is both the existence check and, by accident, the
  authorization boundary, because `demo_manifest` happens to contain only the demo set. A real user's
  league will not be in that manifest until S4 catalogs it. S2a must not deepen the coupling; S2b separates it.
- **Do not let the fast cadence compress this.** An isolation bug does not announce itself — the app looks
  perfect while showing the wrong person's data. The adversarial matrix in S2b is the deliverable, not a
  formality around it.

---

# AMENDMENT — 2026-08-09 — Code's three S2a questions, answered

**These decisions supersede the corresponding lines above.** Written the same turn they were made
(CODING_BIBLE §7). Code raised all three from inside the worktree; the PM had independently found #1 and
the `is_mine` coincidence in #4.

## 1. The current-season term has no data to bite on — env-first override, matrix runs twice

**The problem.** Sleeper `/v1/state/nfl` returns `season: "2026"` (fetched live 2026-08-09). The corpus tops
out at **2025** — all 31 slices in `demo_slate.csv` are 2020–2025. So `visible = demo OR (owned AND season
== current)` collapses today, for every caller, to `visible = demo`. Proof steps 2 and 3 and DoD 3/5 are
unprovable as written, and **gotcha #1 above ("grant user B a corpus league") contradicts the season term —
ignore that line.**

**Decision: `current_season()` resolves env `CURRENT_SEASON` first, then Sleeper `/v1/state/nfl` (cached)** —
the same env-first idiom as `ACCESS_CODE` and `DEMO_LEAGUE_ID`. Rejected: a test-only monkeypatch, because
it leaves the owned-league path never exercised against deployed code, and *that path is the entire session*.

Conditions on the override — all of them are part of DoD:

- **It is process env only.** Never a request parameter, header, query string, or cookie. Assert this.
- **The Sleeper failure mode must be specified and proven, and it must fail CLOSED.** If Sleeper is
  unreachable and there is no cached value, the resolver must not degrade to "any season" — visibility drops
  to demo-only. An outage that costs a user their own league for an hour is an availability bug; one that
  makes every league visible is the exact silent isolation failure this session exists to prevent. Persist a
  last-known-good value, prove the outage path with a simulated failure.
- **Startup logs the resolved season and its source** (`env` / `sleeper` / `cache`); `scripts/users.py
  --list` prints the same line. An override left set must be impossible to miss.
- **Plain `[env]` in fly.toml, not a Fly secret** — this is config, and hiding it is the failure mode.
- **Exercise it on the deployed app, then remove it.** Set `CURRENT_SEASON=2025` on prod, deploy, run the
  full A/B matrix against https://surplusff.com/, then unset, redeploy, and show the resolved season is 2026
  from Sleeper with the override absent. Two extra deploys is the right price in the security session.
- **The session ends with the override unset, demonstrated** — not merely "we didn't set it."

Run the matrix twice, same accounts, same grants, one variable changed:

| | `CURRENT_SEASON=2025` | unset (Sleeper → 2026) |
|---|---|---|
| signed out | demo only | demo only |
| user A | demo + Trap 2025, **A's league first** | demo only |
| user B | demo + YPFL 2025, **never Trap** | demo only |
| A also granted Trap **2024** | hidden — the term bites | hidden |
| demo | visible | visible — **survives the season term** |

Grants: **A → Trap 2025 `1207735666645946368`** · **B → YPFL 2025 `1257433118135037952`** ·
**prior-season fixture → Trap 2024 `1051229073923584000`** to A.

## 2. The `App.jsx` catalog race — fix it in S2a, bounded

**Decision: fix it here.** DoD 5 ("a signed-in user with a league gets their own first") and DoD 8 (live
confirmation on surplusff.com) are *product* claims. Proven by `curl` they demonstrate the API and leave the
deployed site showing a signed-in user the demo — a visibly wrong landing state shipped by the session whose
job is to make an account mean something.

**In scope:** resolve the session before the first catalog fetch; refetch on `SIGNED_IN`; on `SIGNED_OUT`
refetch **and clear the selected `league_id` and `viewer_roster_id`, not just the list**; render a loading
state rather than defaulting to the demo while the session resolves — a flash of the demo is both a wrong
state and the symptom that would hide this regression's return. Handle `TOKEN_REFRESHED` without a refetch
storm.

**The sign-out half gets its own proof, and it is the more serious one:** sign in as A, confirm A's league
is selected, sign out **without a page reload**, show catalog *and* selection both revert to demo-only. A
stale catalog surviving sign-out is a previous-person's-data-still-on-screen bug.

**Out of scope, still S2b:** removing the season selector, flattening the lineage→seasons grouping, any
other `App.jsx` restructuring. **If the change exceeds ~40 lines of `App.jsx`, stop and report rather than
growing** — an unbounded frontend rewrite inside the security session is how the proof budget gets spent on
the wrong thing.

## 3. Test accounts — two disposable `+tags`, and Will's real account as the untouched third state

**Decision: two new addresses** — `willdaniel.wrd+s2a-a@gmail.com` and `willdaniel.wrd+s2a-b@gmail.com` —
**both deleted at the end**, which is also how DoD 7 (`ON DELETE CASCADE` leaves no orphan `user_leagues`
rows) gets demonstrated.

Rejected "Will's real account as A": the realism it buys is small — Code mints both sessions via admin
`generate_link` + `/auth/v1/verify` either way, so a pre-existing A does not exercise the signup flow. The
cost is real: it writes a live grant onto the account that serves as the human baseline, and A cannot be
deleted, so every later session's "what does Will actually see" starts contaminated.

**The addition that makes this better than either option: Will's real account, with zero grants, is the
fixture for the one state the A/B matrix cannot cover** — *signed in, no league yet → the demo as the empty
state*, a row in the settled model's table that nothing else in the proof touches. **That is the live
eyeball for S2a**: after the final deploy, Will signs in at https://surplusff.com/ and gets the demo empty
state, not a 404, not a blank surface.

Any script minting sessions with the service-role key must not commit the key and must skip cleanly when it
is absent.

## 4. Unasked — decision #5 is currently unobservable, and would be reported as proven

`DEMO_LEAGUE_ID` = `1182101676608823296` = LoRP 2025 = `config.SLEEPER_LEAGUE_ID`, flagged `is_mine=true` in
`demo_slate.csv`. So decision #5 above — signed-out resolves to the demo *explicitly* rather than falling
through `MY_USERNAME` to the is_mine league — **produces the identical league before and after the fix.** A
report saying "signed-out now resolves to the demo, demonstrated" would be true and would prove nothing.

**Prove it by temporarily pointing `DEMO_LEAGUE_ID` at a non-mine league** (Trap 2025) and showing the
signed-out default follows the config, not the username. Restore it afterwards. Say so explicitly in the
report — this is precisely the "check the report's strongest verb" case.
